name: Benchmarks
on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: benchmarks-${{ github.ref }}
  cancel-in-progress: true

jobs:
  synthetic:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
      - name: Linux build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            cmake \
            libgtk-3-dev \
            libxcb-render0-dev \
            libxcb-shape0-dev \
            libxcb-xfixes0-dev \
            libxkbcommon-dev \
            nasm
      - uses: dtolnay/rust-toolchain@2fe4ca74464c5902a4f6e302d0a619b4ea911ccc # 1.96
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2
        with:
          shared-key: benchmarks
          save-if: ${{ github.ref == 'refs/heads/main' }}
      - name: Record benchmark environment
        shell: bash
        run: |
          rustc -Vv
          uname -a
          lscpu
          mkdir -p target
          bench_commit="$(git rev-parse HEAD)"
          bench_rust="$(rustc -Vv)"
          bench_os="$(uname -a)"
          bench_cpu="$(lscpu | awk -F: '/Model name/ {sub(/^[ \t]+/, "", $2); print $2; exit}')"
          bench_cores="$(nproc)"
          bench_memory="$(awk '/MemTotal/ {print $2 * 1024; exit}' /proc/meminfo)"
          bench_lock_hash="$(sha256sum Cargo.lock | awk '{print $1}')"
          rawler_commit="$(git -C thirdparty/dnglab rev-parse HEAD)"
          rawler_lock_hash="$(sha256sum thirdparty/dnglab/Cargo.lock | awk '{print $1}')"
          jpeg_bakeoff_lock_hash="$(
            sha256sum tools/jpeg-bakeoff/Cargo.lock | awk '{print $1}'
          )"
          jq -n \
            --arg commit "$bench_commit" \
            --arg cargo_lock_sha256 "$bench_lock_hash" \
            --arg rawler_commit "$rawler_commit" \
            --arg rawler_cargo_lock_sha256 "$rawler_lock_hash" \
            --arg jpeg_bakeoff_lock_sha256 "$jpeg_bakeoff_lock_hash" \
            --arg rustc "$bench_rust" \
            --arg os "$bench_os" \
            --arg cpu "$bench_cpu" \
            --argjson logical_cores "$bench_cores" \
            --argjson memory_bytes "$bench_memory" \
            --arg rayon_threads "${RAYON_NUM_THREADS:-default}" \
            '{
              schema_version: 2,
              commit: $commit,
              cargo_lock_sha256: $cargo_lock_sha256,
              rawler_commit: $rawler_commit,
              rawler_cargo_lock_sha256: $rawler_cargo_lock_sha256,
              jpeg_bakeoff_lock_sha256: $jpeg_bakeoff_lock_sha256,
              rustc: $rustc,
              os: $os,
              cpu: $cpu,
              logical_cores: $logical_cores,
              memory_bytes: $memory_bytes,
              rayon_threads: $rayon_threads,
              raw_fixture: null,
              cache_condition: "Criterion warm-up; operating-system cache not reset",
              power_mode: "GitHub-hosted runner (not controlled)"
            }' > target/benchmark-metadata.json
          cat target/benchmark-metadata.json
      - name: Run core synthetic benchmarks
        run: cargo bench -p viewr-core --features benchmarks --bench core_hot_paths --locked -- --noplot
      - name: Run filmstrip scaling benchmarks
        run: cargo bench -p viewr --features benchmarks --bench filmstrip_scaling --locked -- --noplot
      - name: Run application event-backlog benchmarks
        run: cargo bench -p viewr --features benchmarks --bench event_backlog --locked -- --noplot
      - name: Run generic RAW pipeline benchmarks
        env:
          CRITERION_HOME: ${{ github.workspace }}/target/criterion-rawler
        run: cargo bench --manifest-path thirdparty/dnglab/rawler/Cargo.toml --locked --bench perf -- --noplot
      - name: Run JPEG codec bake-off
        run: cargo bench --manifest-path tools/jpeg-bakeoff/Cargo.toml --locked --bench encode -- --noplot
      - name: Upload Criterion results
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: criterion-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            target/criterion
            target/criterion-rawler
            tools/jpeg-bakeoff/target/criterion
            target/benchmark-metadata.json
          if-no-files-found: warn
          retention-days: 90
